plugins {
    id 'java'
}

group = 'de.groupxyz'
version = '1.6-MAIN'

repositories {
    mavenCentral()
    maven {
        name = "papermc-repo"
        url = "https://repo.papermc.io/repository/maven-public/"
    }
    maven {
        name = "sonatype"
        url = "https://oss.sonatype.org/content/groups/public/"
    }
}

dependencies {
    compileOnly "io.papermc.paper:paper-api:1.21-R0.1-SNAPSHOT"
}

task publishToGitHub(type: Exec) {
    dependsOn jar
    
    workingDir "."
    
    doFirst {
        println "Kopiere aktuelle JAR-Datei..."
        copy {
            from "build/libs/${project.name}-${project.version}.jar"
            into "github-upload/movingblocks"
        }
        
        println "Kopiere src und build.gradle..."
        copy {
            from "src"
            into "github-upload/movingblocks/src"
        }
        
        copy {
            from "build.gradle"
            into "github-upload/movingblocks"
        }
        
        println "Bereite Git-Befehle vor..."
    }
    
    def gitHubUser = project.findProperty('gpr.user') ?: System.getenv('GITHUB_USERNAME') ?: ""
    def gitHubToken = project.findProperty('gpr.key') ?: System.getenv('GITHUB_TOKEN') ?: ""

    if (System.getProperty('os.name').toLowerCase().contains('windows')) {
        def commitCommand = "git add github-upload/movingblocks/* github-upload/movingblocks/src/* && git commit -m \"Update MovingBlocks Plugin to version ${project.version}\""
        
        if (gitHubUser && gitHubToken) {
            commandLine 'cmd', '/c', "${commitCommand} && git -c credential.helper= -c credential.helper='!f() { echo username=\"${gitHubUser}\"; echo password=\"${gitHubToken}\"; }; f' push origin master"
        } else {
            commandLine 'cmd', '/c', "${commitCommand} && git push origin master"
        }
    } else {
        def commitCommand = "git add github-upload/movingblocks/* github-upload/movingblocks/src/* && git commit -m \"Update MovingBlocks Plugin to version ${project.version}\""
        
        if (gitHubUser && gitHubToken) {
            commandLine 'sh', '-c', "${commitCommand} && git -c credential.helper= -c credential.helper='!f() { echo username=\"${gitHubUser}\"; echo password=\"${gitHubToken}\"; }; f' push origin master"
        } else {
            commandLine 'sh', '-c', "${commitCommand} && git push origin master"
        }
    }
    
    ignoreExitValue true
    
    doLast {
        println "GitHub-Update abgeschlossen."
    }
}

task prepareUploadFolder {
    doFirst {
        println "Erstelle Upload-Ordner..."
        mkdir("github-upload/movingblocks")
        mkdir("github-upload/movingblocks/src")
    }
}

publishToGitHub.dependsOn prepareUploadFolder
build.finalizedBy publishToGitHub

task showVersion {
    doLast {
        println "Aktuelle Version: ${project.version}"
        println "Jeder Build lädt die Änderungen automatisch zu GitHub hoch."
        println "Um eine neue Version zu bauen, führe 'gradlew clean build' aus."
    }
}

defaultTasks 'showVersion'

def targetJavaVersion = 21
java {
    def javaVersion = JavaVersion.toVersion(targetJavaVersion)
    sourceCompatibility = javaVersion
    targetCompatibility = javaVersion
    if (JavaVersion.current() < javaVersion) {
        toolchain.languageVersion = JavaLanguageVersion.of(targetJavaVersion)
    }
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'

    if (targetJavaVersion >= 10 || JavaVersion.current().isJava10Compatible()) {
        options.release.set(targetJavaVersion)
    }
}

processResources {
    def props = [version: version]
    inputs.properties props
    filteringCharset 'UTF-8'
    filesMatching('plugin.yml') {
        expand props
    }
}